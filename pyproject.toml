[project]
name = "n8n-experimentation"
version = "0.1.0"
description = "CLI toolkit for n8n workflow automation via Claude Code"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.28.1",
    "python-dotenv>=1.2.2",
]
